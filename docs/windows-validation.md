# Windows 0.7.0 验证记录

记录日期：2026-09-13。本文件区分生成素材回归、真实出版物的只读抽样与安装器验证。测试代码和生成器随源码提交；真实漫画名称、路径、正文和图片不写入本文件或提交 Git。

## 环境与最终结果

- .NET SDK：10.0.401；目标框架：`net10.0-windows10.0.19041.0`；发行架构：`win-x64`。
- 运行环境：Windows 10 2004（19041）及更新版本，或 Windows 11，x64。
- 原生 WPF 阅读画布，自带 .NET、ImageMagick 与 PDFium；EPUB 不使用网页阅读模式，程序包不含 WebView2 阅读组件。
- 安装器：官方 Inno Setup 6.7.3；下载文件已校验固定 SHA-256 与 Pyrsys B.V. Authenticode 签名。

| 检查 | 本轮结果 | 入口 |
| --- | --- | --- |
| 核心回归 | **Release：71 项通过，0 项失败** | `scripts/windows-test.ps1` |
| 原生 WPF 界面集成 | **Release：65 项通过，0 项失败** | `scripts/windows-ui-test.ps1` |
| 原生安装器 | **23 项通过，0 项失败；此前 0.7.0 原生程序文件验证** | `scripts/windows-installer-test.ps1` |
| v4 当前匿名样本实际布局 | **226 页、225 条相邻接缝；最终 13 组，前 50 页 3 组** | 本地只读接缝与 `LayoutEngine` 审计 |
| v4 当前样本局部画面检查 | **前 50 页中的 3 组已查看实际合成图，连续性与物理左右正确** | 本地私有合成检查，图像不提交 |

以上 Core/UI 数字来自包含 `seam-scale-offset-v4-antialias` 的本机 Release 全套实际输出，含 16 项后台扫描、异步边界与增量应用窗口检查，以及 3 项普通双页窗口检查。此前 v3 的 4 册取样与 227 页整本缓存检查单独保留为历史证据，不作为 v4 的本轮成绩。

## 格式与页面身份

生成素材回归覆盖：

- ZIP/CBZ 直接读取，嵌套目录、UTF-8 和常见 GB18030 中文路径、1/2/10 自然排序与隐藏元数据过滤。
- 文件夹或 ZIP 插入排序更早的图片后，已有页面身份与阅读定位不变；损坏图片保留位置；非法路径、损坏包和无图片包被拒绝。
- 单张图片、递归图片文件夹与 TIFF 多帧的真实渲染；TIFF 第二帧、尺寸相关解码缓存、冻结位图与大图资源上限。
- PDF 页数、尺寸、栅格内容、密码错误与正确密码、取消渲染、关闭后释放源文件，以及多个文档的独立生命周期。
- 无 DRM 的 MOBI 6 图片漫画：未压缩与 PalmDOC、封面、正文图片、重复引用和缺失位置；不支持或损坏格式明确报错。

PDFium 保留自 0.6.0 的实现选择：此前 Windows 系统 PDF 路径在远端检查结束时出现原生退出异常，定位后改用随程序分发的 PDFium，串行化原生调用并显式释放页面、位图与文档。该历史不表示当前仍使用系统 PDF 或网页渲染。

## 原生 EPUB 与页面朝向

EPUB 现在面向图片漫画，遵循 spine 顺序，在原生画布显示 DOM 图片、SVG 图片引用和可提取的 CSS 背景图。重复引用、同一页多张图片和缺失资源位置保留；纯文字内容与无法提取图片的页面显示原生提示，不执行 HTML/CSS 文字排版或脚本。

回归检查覆盖匹配当前元素及祖先的旋转规则、层叠与明确的 0°、SVG 与容器旋转；无关样式不能旋转整本书。朝向优先级为：

1. 用户的手动修正。
2. 当前页明确的 EPUB 旋转标记，包含 0°。
3. 仅在没有标记时使用已启用的自动识别。

明确标记不受 OCR 推断覆盖，关闭自动识别仍遵循该标记。CJK 分析参考 macOS 字形方法，区别文字排列方向与字符自身方向；生成素材检查覆盖横排、竖排、多角度、第二种字体，以及对称重复装饰和无文字图案的拒绝。Windows OCR 语言不可用时会明确记录跳过；跳过不算相应字形检查通过。

真实 WPF 检查使用原生 EPUB 页面，验证修正控件可用、旋转确实改变画布内容、配对/交换/解除配对、保存修正、阅读方向变化时确认双图的物理左右不变，以及窗口一次关闭。

## 从当前页开始的逐页分析与缓存

新增的核心与真实窗口检查覆盖以下链路：

- 打开书籍后从当前阅读位置向后逐页分析，再补齐之前的页面，不要求用户逐页翻阅。分析图片最长边为 1100 像素；仅保留必要的当前和前一张分析图，不把全书解码图放入内存。
- 已完成的朝向与相邻接缝结果写入本地 `analysis/` 缓存。缓存身份包含源修订、页面定位、明确的出版物朝向、算法版本及 OCR 语言环境；不匹配的旧缓存不能复用。
- 中途取消或关闭时保留已完成检查点；再打开从未完成部分继续。失败页面不伪装成已完成，后续页面继续分析，未完成项可以重试。
- 完整缓存再次打开时不重新进行分析渲染；重新分析清理的是自动结果，不修改进度、书签或手动修正。
- 后台解码与文档关闭串行协调，OCR 不占用前台文档 I/O 锁；前台翻页优先。切换书籍与一次关闭请求均取消并等待旧任务，释放旧书句柄。
- 每完成一页方向或一条相邻接缝，就向阅读窗口发布结果，立即用于布局；用户停留在当前页时也继续扫描后续内容。生成书的远处方向与连续接缝有相应覆盖，不依赖用户逐页导航。
- 新结果影响当前画面时自动补绘，不再提供“应用识别”按钮。结果在原画面绘制过程中到达时，也会在后续绘制中应用，不要求再次点击或翻页；手动修正仍保持最高优先级。
- 更新当前画面保留用户的阅读定位，不主动跳到后台正在分析的页面。界面检查覆盖逐项应用、绘制期间结果到达、切书与取消边界。

### v3 历史整本扫描与缓存审计

在本轮 v4 之前，使用 v3 算法对另一匿名样本完成了实际后台服务审计，结果为 **227/227 页、226/226 条相邻接缝、失败页 0**，耗时 **187.5 秒**。发生 **226 次分析渲染**，封面无需分析渲染。

随后以完整缓存重新运行同一本书：命中 227 页与 226 条接缝，**分析渲染 0 次**，缓存中的决策与首次完成结果一致。此记录验证当时的完整扫描与缓存复用，不是 v4 耗时或命中数量；算法版本变化后会重新计算，不能直接复用 v3 接缝结果。

## 普通双页与智能跨页证据

普通“双页”模式按阅读方向并排显示相邻页；它与通过接缝证据合成的智能跨页是不同的布局结果。生成回归检查普通双页不会冒充智能接缝、候选左右竞争、单页模式下完整跨页仍可成立、手工修正优先，以及每个来源页只消费一次。手工确认的左右位置独立于之后的阅读方向变化。

### v4 本轮实际样本

当前接缝算法版本是 **`seam-scale-offset-v4-antialias`**。页面缩小到接缝分析尺寸时改用面积抗混叠，随后对边缘轮廓做 `[1, 2, 1] / 4` 三点轻量平滑，减少网点纹理因采样产生的干扰。严格与积极模式的原有分数、相关性、误差、分区和左右差距阈值均保留；没有通过降低阈值增加数量。生成回归包含这类采样与纹理干扰。

对本轮报告漏检的匿名样本完成只读审计：**226 页、225 条相邻接缝**。旧版完整缓存通过实际布局规则得到 **6 组**，前 50 页为 **0 组**；v4 经过朝向、邻居竞争与实际 `LayoutEngine` 分组后得到 **13 组**，保留旧版的全部 6 组。

前 50 页中最终得到 **3 组**：**8–9、10–11、49–50 页**（阅读器的 1 起始页码）。这三组的实际私有合成图已经查看，画面连续、物理左右正确；均为 `Swapped=true`，即后一来源页在左、前一来源页在右。该检查不等于全部 13 组均经人工核对，也不代表已知真实跨页的全部数量或通用识别准确率。原图和合成诊断图仅保留在忽略的本地 `build/` 中。

### v3 历史取样

此前 v3 使用运行时一致的 **1100 像素**分析输入，对 **4 册、888 页、884 条相邻接缝**进行只读分析，解码失败为 0。实际页面经渲染、朝向分析、接缝分析与布局约束后，积极策略最终为 **17 组**，严格策略为 **2 组**；17 组集中在其中一册，另三册为 0 组。这些数字来自旧算法，不能写为 v4 本轮结果。该历史检查未逐组完成 `MainWindow` 画布像素比对或人工出版物意图确认。

原始漫画及其页面图像没有修改，也不作为公开测试素材提交。公开回归依靠生成的连续画面、噪声接缝、偏移/缩放、空白、重复图和对称图案验证接受与拒绝规则。

## 系列书库与记录保护

真实 WPF 书库检查覆盖空态、文件/目录导入、封面位图实际加载、默认系列总览、点击系列进入分卷、自然卷序、网格/列表切换、分页、搜索、收藏、状态筛选和元数据编辑。

来源目录增量刷新、缺失与恢复、停止扫描、重新打开书库和从封面打开阅读器均有独立检查。进度与书库元数据分开合并，防止另一窗口的旧阅读快照覆盖手动书名、系列、标签、收藏与阅读状态。原子保存、备份恢复、跨实例合并和移除记录保留原书继续覆盖。

系列沿用 macOS 的父目录分组。卷序按 macOS 的 `卷` / `vol` / `volume` 数字规则，从原文件名提取，再以显示标题自然排序和路径稳定排序；改显示书名不会改变该来源的卷号。纯字符串回归包含小数卷、无卷号回退、与年份等无关数字的区分、手动改名及同名排序，不读取用户漫画。

## 与 macOS 一致的图标

macOS 构建使用 `Resources/AppIcon.icns`；该源文件与已发布 macOS 0.6.0 包内图标逐字节相同。Windows 程序的 `ApplicationIcon` 与安装器的 `SetupIconFile` 都引用 `windows/ShuiMan.Windows/AppIcon.ico`，窗口图标和标题栏图片也使用同一原始图形。

ICO 包含 **16、20、24、32、40、48、64、96、128、256 像素**共 10 档。32/64/128/256 像素的 PNG 数据与 macOS 原始表示逐字节相同；新增尺寸由最近较大原图缩小，已比对解码后的 RGBA 像素并确认透明度保留。标题栏 `AppIcon.png` 原样提取 macOS 的 64 像素 PNG。`scripts/windows-icon.py` 负责可重复生成这些资产。

## 打包、安装、升级与卸载

此前已使用提交 **`aadfe7f` 的 0.7.0 原生应用 publish 文件**实际编译并完成 **23 项隔离安装器检查，0 项失败**。此结果验证以下安装器流程；本轮图标与 v4 分析更新后的最终程序包由新的 Windows CI 执行安装器检查，不能据此声称已在用户电脑安装验证最新包：

- 两个升级测试包及其 SHA-256 一致性。
- 离线原生程序、.NET、PDFium 和许可文件安装；安装目录没有浏览器阅读组件。
- 旧脚本安装辅助文件的移除、Windows 已安装应用登记、开始菜单和可选桌面快捷方式。
- 默认不关联文件；选择后注册“打开方式”，卸载只移除自己的入口。
- 覆盖升级更新已有应用登记与安装元数据。
- 升级和卸载都保留生成的原书占位文件与书库记录；卸载移除自己安装的程序文件、快捷方式与登记。

测试使用 `ShuiMan.Installer.NativeSmoke.8BB2BDE2` 独立 AppId，以及 `build/windows/installer-native-smoke/`。升级测试标签 0.7.1 只用于验证覆盖流程，不是另一份应用发行版本。测试不启动用户书库，不删除真实漫画，也不卸载系统共享运行时。

正式产物由 `scripts/windows-build.ps1` 与 `scripts/windows-installer.ps1` 生成：`ShuiMan-Windows-x64.zip`、`ShuiMan-Setup-0.7.0-x64.exe` 及各自的 `.sha256`。最终版本、大小和校验值以发布前重建的产物为准。编译器的官方签名验证不等于水漫安装包已获得应用发布者签名。

GitHub Release 正文留空，附件仅包含 Windows 安装 EXE、Android APK 和 macOS 应用 ZIP；Windows 便携 ZIP 与独立 SHA 文件不作为 Release 附件。自动生成的源码下载由 GitHub 提供。

## 验证边界

- 本地通过不能替代远端 CI；远端结果应以该提交的 Windows 工作流实际记录为准。
- EPUB 原生图片提取不等价于完整电子书排版；纯文字、复杂 CSS 排版和脚本不执行。
- CJK 与其他 OCR 结果受到语言、字体、画质、文字数量及页面构图影响；没有针对全部真实漫画给出准确率承诺。
- 未穷尽所有 PDF 加密方案、图像编码、Windows 显示缩放和 OCR 语言组合。
- 本文件只描述 Windows 实际代码与所列检查，不代表 macOS、Android 同时完成了构建或运行验证。
