# Windows 移植验证

本文件只记录实际完成的检查。回归数据由 `windows/ShuiMan.Checks/Fixtures.cs` 动态生成，不包含用户漫画。

## 2026-09-13 本机构建环境

- .NET SDK：10.0.401。
- 目标：`net10.0-windows10.0.19041.0`；便携发行目标：`win-x64`。
- 回归入口：`scripts/windows-test.ps1`。

## 自动回归结果

执行 `scripts/windows-test.ps1`，**38 项通过，0 项失败**。

覆盖内容：

- ZIP/CBZ 的自然数字排序、嵌套目录、UTF-8 中文路径、GB18030 中文路径和隐藏元数据过滤。
- 文件夹和 ZIP 插入排序更早的图片后，原有图片的定位 ID 不变。
- ZIP 中 PNG/JPEG/GIF/BMP 解码，递归图片文件夹和单张图片。
- 独立 TIFF 与 ZIP 内 TIFF 的多帧展开、不同页面身份和第二帧实际渲染。
- TIFF 转 PNG 的浏览器适配、有效 PNG 解码和尺寸限制；重复渲染缓存复用及不同尺寸请求独立解码。
- EPUB 的 spine 顺序、重复引用、缺失页面占位、稳定页面 ID、阅读方向和目录位置。
- 复杂 EPUB 保留 HTML 正文、CSS 和图片资源供浏览器显示。
- 两页 PDF 的页数、纵横尺寸和实际栅格渲染。
- PDF 关闭后立即释放原文件；渲染取消、已关闭文档拒绝渲染、Unicode 路径、多个文档的独立关闭、全部关闭后重新打开，以及无效文件打开失败后的资源释放。
- 由独立生成器创建的 PDF Standard revision 2 加密文件：未提供密码或密码错误时明确要求密码，正确密码下实际渲染内容与原图一致。该检查不代表所有 PDF 加密方案或密码输入界面均已覆盖。
- 未压缩和 PalmDOC 压缩（含尾部数据）的 MOBI 6：封面、图片引用顺序、重复引用、缺失位置、稳定重开 ID、实际图像渲染和封面不重复插入。
- MOBI DRM、KF8、HUFF、记录边界、未知编码、截断和混合文字排版的明确拒绝。
- 损坏 ZIP、无图片 ZIP 和越界路径 ZIP 的拒绝，以及损坏图片保留后续页面位置。
- 单/双页、左右方向、封面独立、手工跨页/旋转/单独显示的优先级；72 组页数和设置组合全部恰好消费每个源页面一次。
- 原创丰富接缝信号在两种来源顺序下的自动配对；已知偏移和缩放的恢复；空白、重复、平滑边界及非法缓冲区拒绝。
- 书库进度、稳定定位、书签、设置和修正持久化；删除记录保留原漫画；损坏主文件从备份恢复；扫描 ZIP/CBZ；批量导入合并与返回快照隔离。
- 图片卷递归扫描不重复生成子卷；父目录有封面图片时仍能发现子目录中的 EPUB。
- 语义损坏的空书名记录从备份恢复；两个独立书库实例交替保存、更新和删除时不丢失其他记录，也不复活已删除记录。

回归过程中发现并修复了 TIFF 非首帧的 ImageMagick 元数据探测错误；最终通过记录包含独立 TIFF 和 ZIP 中 TIFF 的第二帧渲染。

## 便携构建

发布前的 GitHub 构建发现 Windows 系统 PDF 组件在测试通过后的原生退出异常，分项运行将问题定位到 PDF 路径。因此最终 Windows 版使用随程序分发的 PDFium（PDFiumCore 154.0.8035 / Chromium 8035），对所有原生调用统一串行化，显式关闭页面、位图和文档。相关许可证与依赖声明随便携包分发。

已实际执行 `scripts/windows-build.ps1`，完成 Release、自带 .NET 运行时的 `win-x64` 发布，并产生 `build/windows/ShuiMan/ShuiMan.exe`、`ShuiMan-Windows-x64.zip` 和 SHA-256 校验文件。首次构建压缩包约 83 MiB；以后重新构建的最终大小和校验值以产物为准。

构建和安装 PowerShell 脚本已通过语法检查。可选安装器包含 WebView2 存在性检查和缺失时通过 Windows 包管理器 winget 安装的入口；安装/卸载的实际执行结果不能由源代码检查推断。

最终在本机执行 `scripts/windows-build.ps1`，38 项核心检查通过后生成完整发布包；随后以 Release 配置执行 `scripts/windows-ui-test.ps1`，12 项真实界面集成检查通过。已实际执行发布包的 `install.ps1 -DesktopShortcut`，安装到当前用户的 `Programs/ShuiMan` 并创建快捷方式。本机已存在 WebView2 152.0.4191.66，因此本次未触发运行时补装；卸载脚本未实际执行。

人工可视检查使用原创 ZIP 演示：验证 1、2、10 自然顺序、翻页、书签、手动双图及切换 RTL 后物理左右保持不变。另实际显示了 EPUB 的正文、CSS 和图片，并验证修复了资源延迟处理的重复完成异常。书签、阅读方向和配对信息在关闭后写入了隔离的本地测试书库。

## WebView2 和窗口集成检查

实际执行 `scripts/windows-ui-test.ps1 -Configuration Debug`，使用本机 WebView2 **152.0.4191.66**，**12 项通过，0 项失败**。测试加载应用实际的 `RestrictedBookView` 和 `MainWindow`，采用自动生成的 EPUB、单独的临时浏览器配置和临时书库。

- HTML 正文和本地 CSS 实际显示；PNG 与经转换的 TIFF 的浏览器 `naturalWidth` 正确。
- 文档脚本不执行；外部图片未加载；外部导航被取消，当前页面保持不变。
- EPUB 本地链接报告正确 spine 位置并显示下一页；真实主窗口中的页码同步到第 2 页。
- 清空书籍后浏览器回到空白页；真实主窗口一次关闭请求即完成关闭。
- 资源异步请求完成后没有未处理的 Dispatcher 异常，覆盖此次修复的重复完成 WebView2 deferral 问题。

本次使用 Debug 是为与同时进行手工检查的 Release 程序隔离。测试未安装新的 WebView2，也未访问用户漫画。CI 若缺少 WebView2，会明确记录跳过；只有实际输出通过才计为完成验证。

## 验证边界

- 上述为真实 Windows 原生核心回归，使用生成数据；不代表用户全部真实漫画格式都已验证。
- 复杂 EPUB 既检查了资源保留，也在真实浏览器中验证了上述原创页面；这不能覆盖所有出版物的复杂排版和交互。
- 本回归没有覆盖加密 PDF 密码交互、所有第三方图像编码、Windows OCR 所有语言或不同显示缩放比例。
- 回归不依赖真实用户文件，不覆盖 macOS/Android 的运行结果；UI 自动化覆盖限于上述明确列出的流程。
- GitHub Actions 工作流已加入源代码；远端执行结果只有在实际推送并运行后才能确认。
